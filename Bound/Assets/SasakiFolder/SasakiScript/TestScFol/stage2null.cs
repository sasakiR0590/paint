using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class stage2null : MonoBehaviour
{


    // Start is called before the first frame update
    void Start()
    {
        RenderTexture.active = null;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
